# marketdraft
Attempt at putting the fantasy football bidding war draft into a real application.

Dev Instructions:
1) At root of repository, run "go run ./cmd/web"
2) Visit localhost:4000 on a browser on the dev machine.
